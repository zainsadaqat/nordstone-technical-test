import React from 'react';

const Title = ({ title }) => {
  return (
    <h2 className="my-[88px] tracking-wide font-bold text-4xl text-center">
      {title}
    </h2>
  );
};

export default Title;
